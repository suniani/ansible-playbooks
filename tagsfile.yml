---
- hosts: 172.31.32.34
  become: yes
  tags: mygit
  tasks:
  - name: i want to install git
    apt: 
      name: "{{gitpack}}"
      state: present
  - name: i want to install tree
    apt: 
      name: "{{treepack}}"
      state: present
  - hosts: 172.31.42.252
    tags: mywget
  - name: i want to install wget
    yum: 
      name: "{{wgetpack}}"
      state: present
    
  